<div class="navbar">
  <div>
  <%= link_to root_path do %>
    <i class="fa-solid fa-house-chimney-user"></i>
    <% end %>
  </div>
  <div>
    <h1>Rent'Flat</h1>
  </div>
  <div class="user_options">
    <div>
      <% if current_user != nil %>
        <p><%= current_user.email %></p>
      <% else %>
        <%= link_to "S'inscrire !", new_user_registration_path %>
      <% end %>
    </div>

    <div class="nav-user">
      <ul>
       <%# <% if current_user.avatar.attached? && current_user != nil %>
         <%# <li><%= cl_image_tag current_user.avatar.key, class: "avatar dropdown-toggle" %>
       <%# <% else %>
        <li><%= image_tag "avatar1-yellow.png", class: "avatar dropdown-toggle" %>
        <%# <% end %>
          <ul class="dropdown">
            <% if current_user != nil %>
            <p>. <%= link_to "Mes Apparts' à louer", my_flats_path %></p>
            <p>. <%= link_to "Mes Reservations", my_bookings_path %></p>
            <p>. <%= link_to "Mes Demandes", my_demands_path %></p>
            <p>. <%= link_to "Modifier profil", edit_user_registration_path %></p>
            <p>. <%= link_to "Se déconnecter", destroy_user_session_path, method: :delete %></p>
            <% else %>
               <%= link_to "Connexion", new_user_session_path %>
            <% end %>
          </ul>
        </li>
      </ul>
    </div>
  </div>
</div>
